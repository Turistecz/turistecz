name: CI Baseline
on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main, recovery/** ]
  workflow_dispatch:

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI OK"
