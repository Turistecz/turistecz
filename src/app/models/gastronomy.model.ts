export interface Gastronomy {
  title: string;
  streetAddress?: string;
  postalCode?: string;
  addressLocality?: string;
  tel?: string;     // solo el teléfono, no el fax
  url?: string;
  image?: string;
}


